# carutools
carutools is an R package that provides helpful tools for use by Church Army's Research Unit.

Currently it's main feature is making Church Army brand colours available via simple functions (e.g. `ca_cyan()`). It also includes a number of `ggplot()` scale functions, which are wrappers around default scales that use Church Army colours (and pallettes) by default).
